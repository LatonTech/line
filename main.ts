let LeftLineSensor = 0
let RightLineSensor = 0
basic.forever(function () {
    LeftLineSensor = bitbot.readLine(BBLineSensor.Left)
    RightLineSensor = bitbot.readLine(BBLineSensor.Right)
    if (LeftLineSensor == (false && 0 == 0)) {
    	
    } else {
    	
    }
})
